# 02. 프로젝트 아키텍처

이 문서는 프로젝트의 전체 구조, 파일 간 관계, 씬 트리 구성을 설명합니다.

---

## 1. 파일 구조와 역할

```
godot-test/
├── project.godot              # 프로젝트 설정 파일 (엔진 버전, 렌더러, 물리 등)
│
├── test_scene.tscn            # ★ 메인 씬 - 모든 것이 조합되는 곳
├── test_scene.gd              # 아이템 스폰/리스폰 + 적 스폰 관리
│
├── player.gd                  # 플레이어 로직 (이동, 공격, 스킬, 인벤토리, 사격)
├── player.tscn                # 플레이어 씬 (3D 모델 + 충돌체)
│
├── enemy.gd                   # 적1 로직 (독 공격, 원거리)
├── enemy.tscn                 # 적1 씬 (3D 모델 + HP바)
├── enemy2.gd                  # 적2 로직 (근접, 배회/추격/공격 AI)
├── enemy2.tscn                # 적2 씬 (3D 모델 + 3가지 애니메이션)
├── enemy3.gd                  # 적3 로직 (감전 스턴, 번개 반응)
├── enemy3.tscn                # 적3 씬 (4가지 애니메이션)
├── enemy4.gd                  # 적4 로직 (게으른 수면/배회, 돌진 공격)
├── enemy4.tscn                # 적4 씬 (수면/걷기/달리기/구르기)
│
├── camera_follow.gd           # 카메라 추적 (플레이어 따라가기)
├── skill_bar.gd               # UI: 스킬바 + HP/SP 게이지 + 탄약 표시 + 볼륨 조절
├── inventory_ui.gd            # UI: 인벤토리 (I키, 장착/버리기)
│
├── pickup_item.gd             # 바닥 아이템 (클릭 줍기, 권총)
├── chicken_pickup.gd          # 치킨 회복 아이템 (밟으면 자동)
├── ammo_pickup.gd             # 탄약 픽업 (밟으면 자동)
├── pistol_material.gd         # 권총 머티리얼 (2톤 금속)
│
├── lightning_effect.gd        # 번개 이펙트 (ImmediateMesh 지그재그)
├── lightning_effect.tscn       # 번개 씬 (Node3D + OmniLight3D)
├── flamethrower_effect.gd     # 화염방사기 이펙트 (GPUParticles3D + Area3D)
├── flamethrower_effect.tscn    # 화염방사기 씬 (Node3D)
├── teleport_effect.tscn        # 텔레포트 이펙트 씬
│
├── tree.gd                    # 절차적 나무 (트렁크 + 크라운)
│
├── models/                    # 3D 모델 (Meshy AI 생성)
│   ├── player_rigged_textured.glb
│   ├── enemy_rigging_textured_fixed.glb
│   ├── enemy2_rigging_textured_3motions.glb  # 걷기/달리기/공격 애니메이션
│   ├── enemy3.glb                            # 걷기/달리기/잽/감전반응 애니메이션
│   ├── enemy4.glb                            # 걷기/달리기/수면/구르기 애니메이션
│   └── pistol.glb
│
├── sounds/                    # 효과음
│   ├── lightening_bolt_001.wav   # 번개
│   ├── fire_storm_001.wav        # 화염방사기
│   ├── fast_teleportation_001.wav # 텔레포트
│   ├── gunshot.wav               # 총소리 (Python 합성)
│   ├── gulp.wav                  # 치킨 먹기 (Python 합성)
│   ├── poison_spit.wav           # 독 뱉기 (Python 합성)
│   ├── zombie_groan.wav          # 좀비 그로울 (Python 합성)
│   ├── enemy3_attack.wav         # 적3 공격 사운드 (Python 합성)
│   ├── electrocution.wav         # 감전 반응 사운드 (Python 합성)
│   └── earthquake_attack.wav     # 적4 지진 공격 사운드 (Python 합성)
│
└── addons/godot_mcp/          # 에디터 애드온 (MCP 연동, 게임 로직과 무관)
```

### 파일 쌍(pair) 패턴

Godot에서는 `.gd`(스크립트)와 `.tscn`(씬)이 쌍을 이룹니다:

```
player.tscn  →  씬 구조 (어떤 노드들로 구성되는지)
player.gd    →  로직 (그 노드가 어떻게 동작하는지)
```

씬의 루트 노드에 스크립트를 연결하는 방식입니다. `.tscn` 파일 안에서:
```ini
[node name="Player" type="CharacterBody3D"]
script = ExtResource("1")  ← player.gd 연결
```

### 스크립트만 있는 경우

`camera_follow.gd`, `skill_bar.gd`는 별도 `.tscn` 없이 `test_scene.tscn`의 노드에 직접 연결됩니다:
```ini
[node name="Camera3D" type="Camera3D" parent="."]
script = ExtResource("camera_1")  ← camera_follow.gd 연결
```

---

## 2. 메인 씬 트리 (test_scene.tscn)

게임 실행 시 아래와 같은 씬 트리가 구성됩니다:

```
TestScene (Node3D) ← test_scene.gd (아이템/적 스폰 관리)
│
├── WorldEnvironment                    # 환경 설정 (배경색, 앰비언트 라이트)
│
├── Camera3D (camera_follow.gd)         # 아이소메트릭 카메라
│   - Orthographic 투영 (원근 없음)
│   - 플레이어를 부드럽게 추적
│
├── DirectionalLight3D                  # 태양광 (그림자 활성화)
│
├── Ground (StaticBody3D)               # 바닥 (20x20 크기)
│   ├── MeshInstance3D                  # 녹색 박스 메시
│   └── CollisionShape3D               # 충돌 영역
│
├── Player (CharacterBody3D) ← player.tscn 인스턴스
│   ├── CharacterModel (3D 모델)       # AI 생성 리깅 모델
│   │   ├── Skeleton3D                 # 뼈대 애니메이션
│   │   └── _equipped_model ← 코드     # 장착 권총 (장착 시)
│   ├── CollisionShape3D               # 충돌 영역
│   ├── HPBar (Node3D) ← 코드          # 머리 위 HP 바
│   └── AudioStreamPlayer x4 ← 코드    # 번개, 화염, 텔레포트, 총소리
│
├── Enemy (CharacterBody3D) ← enemy.tscn 인스턴스
│   ├── EnemyModel (3D 모델)           # AI 생성 리깅 모델
│   ├── CollisionShape3D               # 충돌 영역
│   └── HPBar (Node3D)                 # 머리 위 HP 바
│
├── Enemy2 x1 ← test_scene.gd에서 동적 스폰
│   ├── EnemyModel (3D 모델)           # 3가지 애니메이션 모델
│   ├── CollisionShape3D
│   └── HPBar (Node3D)
│
├── Enemy3 x1 ← test_scene.gd에서 동적 스폰
│   ├── EnemyModel (3D 모델)           # 4가지 애니메이션 (감전 반응 포함)
│   ├── CollisionShape3D
│   └── HPBar (Node3D, 보라색)
│
├── Enemy4 x1 ← test_scene.gd에서 동적 스폰
│   ├── EnemyModel (3D 모델)           # 4가지 애니메이션 (수면 포함)
│   ├── CollisionShape3D
│   └── HPBar (Node3D, 주황색)
│
├── Tree (StaticBody3D)                 # 절차적 나무 (tree.gd)
│
├── 동적 스폰 아이템 ← test_scene.gd에서 관리
│   ├── pickup_item (Area3D)            # 권총 픽업
│   ├── chicken_pickup (Area3D) x2      # 치킨 회복
│   └── ammo_pickup (Area3D)            # 탄약 (조건부 스폰)
│
├── SkillBar (CanvasLayer) ← skill_bar.gd
│   └── Control (FULL_RECT)
│       ├── HBoxContainer               # 스킬 슬롯 4개 (우측 하단)
│       │   ├── Slot 0: ⚡ 번개
│       │   ├── Slot 1: 🔥 화염
│       │   ├── Slot 2: 💨 텔레포트
│       │   └── Slot 3: (비어있음)
│       ├── AMMO Label (탄약 표시)       # 권총 장착 시만 표시
│       ├── Volume Control (우측 상단)   # VOL 슬라이더 + 퍼센트
│       ├── HP Gauge (좌측 하단)
│       └── SP Gauge (좌측 하단)
│
└── InventoryUI (CanvasLayer) ← inventory_ui.gd
    └── I키로 토글되는 인벤토리 패널
```

**"← 코드에서 생성"**: `.tscn` 파일에 없고, 스크립트의 `_ready()`에서 동적으로 만들어지는 노드들입니다.

---

## 3. 데이터 흐름

### 입력 → 행동

```
좌클릭:
  → player.gd: _unhandled_input()
    → _handle_click(): 레이캐스트로 뭘 클릭했는지 판별
      → 적 클릭? → _attack_target 설정, 접근 후 근접 공격
      → 아이템 클릭? → _try_pickup() → 인벤토리에 추가
      → 빈 땅? → _move_to_ground()로 이동 목표 설정

우클릭:
  → player.gd: _shoot_pistol(screen_pos)
    → 권총 장착 + 탄약 > 0 + 쿨다운 끝? → 사격!
    → 캐릭터 회전 → 레이캐스트 → 데미지 + 이펙트

키보드 1번: 번개 스킬
키보드 2번 (홀드): 화염방사기
키보드 3번: 텔레포트
I키: 인벤토리 열기/닫기
```

### 데미지 흐름

```
플레이어 → 적:
  근접 공격: 좌클릭 → 크리 판정 → enemy.take_damage()
  권총 사격: 우클릭 → 레이캐스트 → 크리 판정 → enemy.take_damage()
  번개 스킬: 1번키 → 즉시 데미지 + 이펙트
  화염방사기: 2번키 홀드 → 0.5초마다 데미지 틱

적 → 플레이어:
  Enemy(독): 독 투사체 → 포물선 비행 → 착탄 거리 판정 → player.take_damage()
  Enemy2(근접): jab 애니메이션 → 끝에서 거리 판정 → player.take_damage()
  Enemy3(근접): left_jab → 끝에서 거리 판정 → 약한 데미지
  Enemy4(돌진): running → roll_dodge → 강력 데미지 (HP 1/3)
  HP ≤ 0 → _die() → Game Over 화면

특수 상호작용:
  번개 스킬 → Enemy3: apply_electrocution() → 3초 스턴 + 감전 애니메이션

회복:
  치킨 밟기 → player.heal_hp(50)
```

### 프레임 루프

```
매 프레임 (_process):
├── camera_follow.gd: 카메라 위치를 플레이어 쪽으로 보간
├── player.gd: HP 바를 카메라 방향으로 회전 (빌보드)
├── enemy.gd / enemy2.gd: HP 바 빌보드
├── skill_bar.gd: 쿨타임 + HP/SP 게이지 + 탄약 표시 업데이트
├── test_scene.gd: 아이템 리스폰 타이머 관리
├── pickup_item.gd: 아이템 bobbing + 라벨 표시
└── chicken_pickup.gd / ammo_pickup.gd: bobbing + 회전

매 물리 프레임 (_physics_process):
├── player.gd:
│   ├── 공격/스킬/사격 쿨타임 감소
│   ├── 화염방사기 업데이트
│   ├── 공격 대상 추적 및 공격 실행
│   ├── 이동 (velocity → move_and_slide)
│   └── 맵 경계 클램프
├── enemy.gd: 추격 + 독 투사체 발사
├── enemy2.gd: 배회/추격/공격 상태 머신
├── enemy3.gd: 배회/추격/공격 + 감전 스턴 상태
├── enemy4.gd: 배회/수면 사이클 + 돌진 공격
└── flamethrower_effect.gd: 데미지 틱 (0.5초마다)
```

---

## 4. 의존성 관계

```
test_scene.tscn → test_scene.gd
  ├── → player.tscn → player.gd
  │                      ├── → lightning_effect.tscn → lightning_effect.gd
  │                      ├── → flamethrower_effect.tscn → flamethrower_effect.gd
  │                      ├── → teleport_effect.tscn
  │                      ├── → pistol.glb (장착 모델)
  │                      ├── → pistol_material.gd (static 머티리얼)
  │                      └── → sounds/*.wav
  ├── → enemy.tscn  → enemy.gd
  ├── → enemy2.tscn → enemy2.gd
  ├── → enemy3.tscn → enemy3.gd
  ├── → enemy4.tscn → enemy4.gd
  ├── → pickup_item.gd (바닥 권총)
  ├── → chicken_pickup.gd (치킨 회복)
  ├── → ammo_pickup.gd (탄약)
  ├── → camera_follow.gd
  ├── → skill_bar.gd
  │         └── → player (런타임 참조: ../Player)
  └── → inventory_ui.gd
            └── → player (런타임 참조: ../Player)
```

**화살표(→) 의미:**
- `.tscn → .gd`: 씬이 스크립트를 참조
- `.gd → .tscn`: 스크립트가 `preload()`로 다른 씬 로드
- `skill_bar.gd → player`: 런타임에 `get_node("../Player")`로 참조

---

## 5. 설계 원칙

### 1. 씬 = 컴포넌트

각 게임 오브젝트는 독립적인 씬 파일입니다:
- `player.tscn`: 플레이어 캐릭터 (독립적으로 테스트 가능)
- `enemy.tscn` ~ `enemy4.tscn`: 적 캐릭터 4종 (AI 다름, 동일 인터페이스)
- `lightning_effect.tscn`: 일회성 이펙트 (스폰 → 애니메이션 → 자체 삭제)

### 2. 코드에서 노드 생성

이 프로젝트는 많은 노드를 `_ready()`에서 코드로 생성합니다:
- **장점**: `.tscn` 파일이 간결해지고, 동적 생성이 쉬움
- **단점**: 에디터에서 미리볼 수 없음
- **예**: HP 바, 스킬바 UI, 데미지 숫자, 파티클 등

### 3. 이펙트 = 일회용 씬

`lightning_effect`, `damage_number` 등은 스폰되고 애니메이션 후 `queue_free()`로 자체 삭제됩니다:
```gdscript
# 생성
var effect := scene.instantiate()
get_tree().current_scene.add_child(effect)

# (이펙트 내부에서) 애니메이션 끝나면 자체 삭제
tween.tween_callback(queue_free)
```

---

## 다음 단계

[03. 플레이어 이동](03-player-movement.md)에서 클릭 이동과 물리 시스템을 자세히 살펴봅니다.
